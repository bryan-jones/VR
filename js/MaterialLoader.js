/**
 * @file
 * Defines material types.
 */

/**
 * Define the Material Loader class.
 */
var MaterialLoader = function(texture, shine, textureX, textureY, spec) {
  this.texture = texture;
  this.shine = shine;
  this.textureX = textureX;
  this.textureY = textureY;
  this.spec = spec;
};

Material.prototype.build = function() {

};
